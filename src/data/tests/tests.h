#pragma once


#include <iostream>


// #define COMPARE_FAIL_OUT(prefix, expected, result) \
//     std::cout << prefix << "FAIL: Expected(" << expected << ") != result(" << result << ")";

// #define COMPARE(prefix, expected, result) \
//     if (expected != result) \
//         COMPARE_FAIL_OUT(std::string(prefix) + " Compare", expected, result)

// #define SIZE_COMPARE(prefix, expected, result) \
//     COMPARE(std::string(prefix) + "Size", expected, result)

// #define FP_COMPARE(prefix, expected, result, epsilon) \
//     if (std::abs(expected - result) > epsilon) \
//         COMPARE_FAIL_OUT(std::string(prefix) + " Compare", expected, result)

// #define ARRAY_COMPARE \
